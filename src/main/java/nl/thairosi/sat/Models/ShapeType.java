package nl.thairosi.sat.Models;

/**
 * The ShapeType enumeration makes it possible for the shape class to bind the chosen shapeType
 * This way only the shapeTypes inserted in this enum are allowed as the shapeType of a shape object
 */
public enum ShapeType {
    CUBE, CYLINDER, SPHERE, PYRAMID, CONE
}
